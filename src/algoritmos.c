#include "algoritmos.h"
#include "tabela.h"

/*
A função fifo_escolha vai me retornar o indice que deve ser inserido ou substituido baseado no algoritmo fifo
*/
int fifo_escolha(moldura *moldura){
	int escolha = 0;
	int primeiro_acesso = NULL;
	for(int i=0;i<moldura->num_entradas;i++){

	}

}
